apiVersion: v1
kind: Service
metadata:
  name: project-service
spec:
  type: NodePort
  selector:
    app: project
  ports:
    - port: 80           # Port inside the cluster
      targetPort: 80     # Container port
      nodePort: 30001    # Port to access externally
